const Fruits = props => {
    return (
        <div>
            {/* Display fruit name and fruit color */}
            {/* <div>{props.fruit.name} - {props.fruit.color} </div> */}
        </div>
    )
}

export default Fruits;